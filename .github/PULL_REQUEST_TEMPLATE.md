## Summary

<!-- What does this PR do, and why? -->

## Checklist

- [ ] `flutter analyze` passes with 0 issues
- [ ] `flutter test` passes
- [ ] `dart format` run on changed files
- [ ] Follows [CONTRIBUTING.md](../CONTRIBUTING.md): design tokens only
      (no raw `Colors.*`/`fontSize`/radii), both themes checked, new
      user-facing strings added to both ARB files, `flutter gen-l10n` run
      if ARB changed, DB migration + `build_runner` run if schema changed
- [ ] New service/utility logic has test coverage

## Manual testing

<!-- What did you check by hand (device/theme/locale)? Screenshots for UI changes are welcome. -->
